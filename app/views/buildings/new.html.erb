<h1 class="text-center text-secondary my-5">New Building</h1>
<div class="container">

<%= form_with(model: @building) do |form| %>
  <% if @building.errors.any? %>
    <div id="error_explanation">
      <h4><%= pluralize(@building.errors.count, "error") %> prohibited this building from being saved:</h4>

      <ul>
        <% @building.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  
  <%= form.text_field :name, class:'form-control', placeholder: 'Name of building' %><br>
  <br><br>
  <div class="text-center">
  <%= form.submit 'Submit', class: 'btn btn-secondary' %>
  </div>
  <br>
<% end %>
</div>